# Docker Ports Configuration
# Change these if you have port conflicts

# Production (nginx) port - external port for accessing the site
# Default: 8080
# Common alternatives: 3000, 3001, 8000, 8888, 9000
PORT_PROD=8080

# Development (bun dev server) port - external port for development
# Default: 4321
# Common alternatives: 3000, 5173, 8080
PORT_DEV=4321

# OpenAI API Configuration (for old site generator in backup/)
OPENAI_API_KEY=sk-openai-api-key
# OPENAI_API_BASE=https://api.openai.com/v1